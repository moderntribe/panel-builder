
const wp = window.wp || {};
const plupload = window.plupload || {};

export const wpMedia = wp.media || null;
export const PlUploader = plupload.Uploader || null;
export const WPShortcode = wp.shortcode || null;
export const panelBackbone = window.Backbone || {};
